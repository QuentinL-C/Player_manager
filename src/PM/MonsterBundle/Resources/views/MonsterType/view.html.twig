{# src/PM/MonsterBundle/Resources/views/MonsterType/view.html.twig #}
 
{% extends "PMMonsterBundle:MonsterType:layoutAdmin.html.twig" %}

{% block title %}
    {{ monstertype.name }} - {{ parent() }}
{% endblock %}
    
{% block h1 %}
    {{ monstertype.name }} :
{% endblock %}
    
{% block h1_extend %}
    <a class="btn btn-lg btn-primary" href="{{ path('pm_monstertype_administration_edit', {'slug': monstertype.slug}) }}" >Modifier</a>
    <a class="btn btn-lg btn-danger main-JS-delete" href="{{ path('pm_monstertype_administration_delete', {'slug': monstertype.slug}) }}" >Supprimer</a>
{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="active">{{monstertype.name}}</li>
    <li class="active">Voir</li>
{% endblock %} 

{% block PM_body %}
<div class="row">
    <section id="content" class="col-lg-12">
        {% for flashMessage in app.session.flashbag.get('notice') %}
            <div class="alert alert-success">
                {{ flashMessage }}
            </div>
        {% endfor %}
        
        <div class="row">
            <div class="col-md-6">
                {% if monstertype.description is not empty %}
                    <div class="well">
                        {{ monstertype.description|raw }}
                    </div>
                {% else %}
                    <div class="alert alert-warning" role="alert">Aucune description fournie pour ce type de monstre.</div>
                {% endif %}
            </div>
            <div class="col-md-6">
                <h2>Informations pratiques :</h2>
                <ul>
                    <li>A venir</li>
                </ul>
            </div>
        </div>
    </section>
</div>
{% endblock %}