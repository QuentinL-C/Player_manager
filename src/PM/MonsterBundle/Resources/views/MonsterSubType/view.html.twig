{# src/PM/MonsterBundle/Resources/views/MonsterSubType/view.html.twig #}
 
{% extends "PMMonsterBundle:MonsterSubType:layoutAdmin.html.twig" %}

{% block title %}
    {{ monstersubtype.name }} - {{ parent() }}
{% endblock %}
    
{% block h1 %}
    {{ monstersubtype.name }} :
{% endblock %}
    
{% block h1_extend %}
    <a class="btn btn-lg btn-primary" href="{{ path('pm_monstersubtype_administration_edit', {'slug': monstersubtype.slug}) }}" >Modifier</a>
    <a class="btn btn-lg btn-danger main-JS-delete" href="{{ path('pm_monstersubtype_administration_delete', {'slug': monstersubtype.slug}) }}" >Supprimer</a>
{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="active">{{monstersubtype.name}}</li>
    <li class="active">Voir</li>
{% endblock %} 

{% block PM_body %}
<div class="row">
    <section id="content" class="col-lg-12">
        {% for flashMessage in app.session.flashbag.get('notice') %}
            <div class="alert alert-success">
                {{ flashMessage }}
            </div>
        {% endfor %}
        
        <div class="row">
            <div class="col-md-6">
                {% if monstersubtype.description is not empty %}
                    <div class="well">
                        {{ monstersubtype.description|raw }}
                    </div>
                {% else %}
                    <div class="alert alert-warning" role="alert">Aucune description fournie pour ce sous-type.</div>
                {% endif %}
            </div>
            <div class="col-md-6">
                <h2>Informations pratiques :</h2>
                <ul>
                    <li>A venir</li>
                </ul>
            </div>
        </div>
    </section>
</div>
{% endblock %}